import logonh from "@/img/logo-shop.svg"
const Logo = () => {
    return (
        <div>
            <img height={130} width={130} alt="logo" src={logonh} />
        </div>
    );
};

export default Logo;
